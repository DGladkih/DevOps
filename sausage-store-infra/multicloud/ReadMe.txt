# Мультиоблачная архитектура "Сосисочной"

## ������ Описание архитектуры

Основной кластер Kubernetes развёрнут в **Yandex Cloud**:
- 1 Master Node
- 2 Worker Node
- Управляемый кластер MongoDB для хранения данных

Приложение масштабируется и использует возможности других облаков:

- В **AWS** подключается через **API Gateway**, взаимодействует с дополнительным кластером **Managed MongoDB**, реплицируя или бэкапируя данные.
- В **Google Cloud (GCP)** используются **Compute Engine** и сервисы **IaaS/PaaS/SaaS** (например, ML API, BigQuery, облачные функции) для расширенных возможностей.

---

## ✅ Плюсы

+ **Отказоустойчивость**: сбой одного облака не останавливает всё приложение.
+ **Гибкость**: возможность выбирать лучшие инструменты от каждого провайдера.
+ **Надёжность хранения**: данные дублируются между Yandex и AWS.
+ **Масштабируемость**: можно быстро масштабировать рабочие ноды или сервисы в нужном облаке.
+ **Безопасность**: разные облака можно использовать для разделения уровней (приложение/данные/API).

---

## ❌ Минусы

- Повышенная **сложность в настройке и поддержке**.
- **Увеличенные затраты**: особенно на трафик между облаками.
- Сложность в организации **мониторинга**, **логирования** и **CI/CD** в мультиоблачной среде.
- Требуются знания и доступ к 3+ облачным платформам.

---

## ������ Особенности

- Все воркеры Kubernetes работают в Yandex Cloud, но часть бизнес-логики может выноситься в другие облака.
- MongoDB работает как централизованное или распределённое хранилище (в зависимости от стратегии — репликация, шардирование, бэкапы).
- Интеграция через API (например, API Gateway в AWS) позволяет безопасно управлять взаимодействием между облаками.
- Compute ресурсы в GCP используются для ресурсоёмких задач (например, аналитика, генерация отчётов, ML).

---

## ������ Содержимое директории `multicloud`

- `multicloud.jpg` — схема архитектуры мультиоблака.
- `ReadMe.txt` — описание преимуществ, недостатков и структуры решения.

